/**
 * 
 */
package org.openmrs.module.teammodule.util;

import java.net.HttpURLConnection;
import java.net.MalformedURLException;
import java.net.URL;

/*import javax.servlet.http.HttpServletRequest;

//import org.dbunit.util.Base64;
import org.openmrs.api.context.Context;
import org.openmrs.util.HttpClient;*/



/**
 * @author Muhammad Safwan
 * 
 */
public class HttpRequest {

	// DefaultHttpClient client = new DefaultHttpClient();

	/*HttpServletRequest request;
	Context context;

	public String executePost(String content) {
		URL url;
		HttpURLConnection connection = null;
		String targetURL = "/openmrs/ws/rest/v1";
		String auth = "";
		try {
			HttpClient client = new HttpClient(content);
			// HttpPost httpPost = new HttpPost(targetURL);
			url = new URL(targetURL);
			// auth = Base64.encodeString(request.getParameter("username") + ":"
			// + request.getParameter("password"));

		} catch (MalformedURLException e) {
			e.printStackTrace();
		}
		return null;

	}*/

}
